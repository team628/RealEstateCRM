# OWNER ACTION QUEUE

## REQUIRED NOW
*(nothing — no owner action currently blocks development)*

## REQUIRED BEFORE FEATURE ACTIVATION
- **OA-002:** Provision Supabase staging project; provide URL + anon key (activates real auth/data).
- **OA-003:** AI provider API key (activates AI features when built).
- **OA-004:** Email + SMS provider accounts; start A2P 10DLC registration early (weeks of lead time).
- **OA-005:** MLS access agreement / IDX vendor selection.

## REQUIRED BEFORE PRODUCTION
- **OA-001:** Review `docs/MASTER_SPEC.md`. The original product specification was
  never committed to this (empty) repository; the spec in use is derived from the
  execution directive. Confirm or correct scope. *(Development proceeds on the
  derived spec meanwhile.)*
- **OA-006:** Separate production Supabase project + backup/PITR plan confirmation.

## OPTIONAL
- **OA-007:** Brand assets (logo, colors) for the app shell.
